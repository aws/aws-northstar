import { Meta } from '@storybook/addon-docs';
import Link from '@cloudscape-design/components/link';
import { Prism as SyntaxHighlighter } from 'react-syntax-highlighter';

<Meta title="Migration/FormGroup" />

# Migrating from NorthStar legacy - FormGroup

There is not a matched FormGroup component from Cloudscape. For a list of Checkboxes, use 
a array of <Link href='https://cloudscape.design/components/checkbox' target='_blank'>Cloudscape Checkbox component</Link> to build the FormGroup.
